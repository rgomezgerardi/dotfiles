# -*- mode: org -*- #
:PROPERTIES:
:header-args+: :tangle config
:END:

Fringe setup and control

#+begin_src emacs-lisp
(use-package fringe :straight (:type built-in)
)
#+end_src

(setq set-fringe-mode 10)        ; Give some breathing room
