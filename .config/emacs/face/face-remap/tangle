# -*- mode: org -*- #
:PROPERTIES:
:header-args+: :tangle config
:END:

Functions for managing `face-remapping-alist'

#+begin_src emacs-lisp
(use-package face-remap :straight (:type built-in)
	:demand
	:general
    ("<C-wheel-up>" 'text-scale-increase)
    ("<C-wheel-down>" 'text-scale-decrease)
)
#+end_src
